import styled from '@emotion/styled';

export const LocatorContainer = styled.div`
    width: 100%;
    height: 62px;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    padding: 10px 10px;
`;

export const LocatorTitle = styled.div`
    width: 46%;
`;

export const LocatorCloseButton = styled.button`
    width: auto;
    height: auto;
    border: none;
    background: #fff;
`;
